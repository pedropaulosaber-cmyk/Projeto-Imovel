# dependências
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# ambiente — nunca versionar segredo
.env
.env.*
!.env.example

# testes e cobertura
/coverage
/.vitest

# prisma
/prisma/*.db
/prisma/*.db-journal

# sistema
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# Cliente Prisma gerado — recriado por `prisma generate` (parte do
# script de build). Versionar isto sincroniza mal com o schema e infla
# o repositório com ~11MB de binário/wasm sem necessidade.
/src/generated/
