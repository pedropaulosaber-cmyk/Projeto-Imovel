/**
 * Substituto de `server-only` nos testes. Ver a nota em `vitest.config.ts`.
 *
 * Vazio de propósito: o pacote original não exporta nada, ele só lança quando
 * carregado no ambiente errado.
 */
export {};
